services:
  web:
    image: kunwarrajesh2407/notes-app:latest
    container_name: web_cont
    ports:
      - "8080:8000"   # Changed from 80:80 to avoid port conflict
    depends_on:
      - django_app
    restart: unless-stopped
    networks:
      - notes-app-nw

  django_app:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: django_cont
    command: >
      sh -c "python manage.py migrate --noinput &&
             gunicorn notesapp.wsgi:application --bind 0.0.0.0:8000"
    env_file:
      - .env
    ports:
      - "8000:8000"
    depends_on:
      db:
        condition: service_healthy
    restart: unless-stopped
    healthcheck:
      test: ["CMD-SHELL", "curl -f http://localhost:8000/admin || exit 1"]
      interval: 15s
      timeout: 5s
      retries: 5
      start_period: 40s
    networks:
      - notes-app-nw

  db:
    image: mysql:8.0
    container_name: db_cont
    environment:
      MYSQL_ROOT_PASSWORD: root
      MYSQL_DATABASE: test_db
    volumes:
      - mysql_data:/var/lib/mysql
    restart: unless-stopped
    healthcheck:
      test: ["CMD", "mysqladmin", "ping", "-h", "localhost", "-uroot", "-proot"]
      interval: 15s
      timeout: 5s
      retries: 5
      start_period: 60s
    networks:
      - notes-app-nw

volumes:
  mysql_data:

networks:
  notes-app-nw:
    driver: bridge
